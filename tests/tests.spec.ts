import { test, expect } from '@playwright/test';

test.describe('Login', () => {
    test('load login page', async ({ page }) => {
        // Have fun! 🎉

        // TEMP: force a fail
        expect(false).toBeTruthy();
    });
});
